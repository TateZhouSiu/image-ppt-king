# Demo

This demo intentionally uses synthetic public content.

Expected flow:

1. Generate visual layers with Image Split from `source.png` and `elements.json`.
2. Write or copy the generated page folder to `visual-layers/slide-01`.
3. Build the PPTX using `text-layer.json`.
